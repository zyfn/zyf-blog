---
title: 把 Codex 接进自己的产品：App Server 使用指南
date: 2026-07-31
lastmod: 2026-08-31
summary: 从一次真实任务出发，理解 Codex App Server 的运行模型、完整命令面和生产客户端边界。
tags:
  - Codex
  - App Server
  - Agent Runtime
  - Agent Infra
cover: /images/posts/codex-app-server-guide/client-architecture.svg
featured: false
draft: false
---

如果你的程序只需要提交一次任务并等待结果，App Server 通常不是第一选择。用 `codex exec`，或者在代码里使用 Codex SDK，会更短、更直接。

App Server 解决的是另一类问题：你正在做一个 IDE、桌面应用、内部研发平台或 Agent Team，需要长期持有多个 Codex 会话，持续接收执行事件，在正确的时刻展示 Diff、审批、用户输入和错误，并在进程或页面重启后恢复原来的工作。

这时，真正困难的不是“怎样发送一段 Prompt”，而是怎样拥有一条可靠的 Agent 运行链路：

~~~text
业务对象
  → Thread
    → Turn
      → Item
        → 工具、文件、审批、用户输入
          → 最终事件
            → 业务验收
~~~

App Server 位于这条链路的中间。它把 Codex Harness 暴露为一个长生命周期进程和一组双向消息，但不会替你的产品拥有业务状态。

<Callout title="本文基线">
本文依据 OpenAI 官方 App Server 文档、Codex 开源实现，以及本机 `codex-cli 0.149.0` 在 2026-08-31 生成的 stable / experimental Schema。版本升级后，应重新生成 Schema，而不是把本文当成永远不变的接口定义。
</Callout>

## App Server 是什么

Codex 不只有一个聊天窗口。CLI、IDE、Desktop 和远程运行环境都需要驱动同一套 Agent Loop：加载上下文、调用模型、执行工具、应用权限、记录事件，并在必要时向用户请求决定。

App Server 把这套 Harness 变成了可以被其他程序控制的边界：

~~~text
你的产品
  ⇅ JSON-RPC lite
Codex App Server
  ⇅
Codex Core Threads
  ⇅
模型 · Shell · 文件 · MCP · Skills · 审批
~~~

它同时是两件事：

- 一个长生命周期进程，负责承载和管理 Codex Thread；
- 一套双向协议，让客户端发送请求、接收通知，并回答服务端反向请求。

<Figure
  src="/images/posts/codex-app-server-guide/app-server-boundary.svg"
  alt="App Server 系统边界：产品拥有 UI 和业务状态，App Server 管理协议与 Thread，Codex 执行模型、工具、文件和 MCP"
  caption="App Server 连接产品和 Codex 执行面，但不会替产品拥有用户、业务 Task、验收与恢复策略。"
/>

官方协议保留 JSON-RPC 的请求、响应和通知形状，但 stdio 使用一行一条 JSON 的 JSONL framing。示例中不需要发送 `"jsonrpc": "2.0"`。

### 什么时候应该使用

适合 App Server：

- IDE 或桌面应用需要嵌入完整 Codex 体验；
- 一个服务需要长期管理多个 Thread；
- UI 要实时展示 Agent 消息、计划、命令输出、文件修改和工具调用；
- 用户要在执行中追加指令或中断 Turn；
- 客户端必须处理命令审批、文件审批、权限申请或 MCP elicitation；
- 页面、客户端或网络断开后仍要恢复同一 Thread；
- 业务系统需要把自己的对象稳定绑定到 Codex 会话。

不必直接使用 App Server：

- 一次性分析、批处理或 CI 任务；
- 只关心最终文本或结构化结果；
- 不需要多轮会话、实时事件或审批 UI；
- 不希望自己维护 JSON-RPC 客户端和事件归并器。

| 方式 | 你实际拥有的东西 | 更适合 |
| --- | --- | --- |
| `codex exec` | 一次命令的执行生命周期 | Shell 自动化、CI、批处理 |
| Codex SDK | 类型化的程序调用接口 | 服务端任务、代码内自动化 |
| App Server | 完整 Harness、Thread、事件和反向请求 | IDE、Desktop、Dashboard、Agent Team |

### 传输怎么选择

本机 `0.149.0` 支持：

| 传输 | 启动方式 | 状态 | 使用建议 |
| --- | --- | --- | --- |
| stdio | `codex app-server` | 默认 | 本地嵌入首选；客户端拉起子进程 |
| Unix Socket | `--listen unix://` 或 `unix://PATH` | 可用 | 同机多客户端共享控制通道 |
| WebSocket | `--listen ws://IP:PORT` | 实验、官方标注 unsupported | 仅 localhost 或 SSH 转发；远程必须 TLS + Auth |
| off | `--listen off` | 可用 | 不暴露本地客户端传输 |

不要把未认证的非 loopback WebSocket 暴露到公网。需要远程连接时，使用 `wss://`，并通过 capability token 或 signed bearer token 完成握手认证。

## 一次任务如何运行

理解 App Server，先不要背接口。先看一条任务从客户端进入 Codex，再回到 UI 的完整路径。

### 三个运行原语

**Thread** 是持久会话。它保存多次 Turn 的历史，可以创建、恢复、分支、归档和读取。

**Turn** 是一次用户输入引发的 Agent 工作。用户要求“修复失败测试”会创建一个 Turn；模型思考、工具调用、命令执行和最终回复都发生在这个 Turn 中。

**Item** 是 Turn 内的一项输入或输出，例如用户消息、Agent 消息、命令执行、文件修改、计划、审批请求和 MCP 工具调用。

~~~text
Thread
├── Turn
│   ├── userMessage
│   ├── commandExecution
│   ├── fileChange
│   └── agentMessage
└── Turn
    └── ...
~~~

边界非常重要：

- `item/completed` 证明一个 Item 到达最终状态；
- `turn/completed` 证明一次 Turn 已结束；
- 两者都不证明你的业务目标已经验收通过。

<Figure
  src="/images/posts/codex-app-server-guide/turn-lifecycle.svg"
  alt="App Server Turn 时序：客户端初始化、创建 Thread、启动 Turn、接收 Item 通知、回答审批并等待 turn completed"
  caption="请求的响应只确认调用，持续通知描述执行过程；审批等反向请求必须由客户端回答，最终以 completed 事件收敛。"
/>

### 连接握手

每条连接都必须先发送 `initialize`，再发送 `initialized` 通知。握手前的其他请求会被拒绝。

请求：

~~~json
{
  "method": "initialize",
  "id": 0,
  "params": {
    "clientInfo": {
      "name": "engineering-console",
      "title": "Engineering Console",
      "version": "0.1.0"
    },
    "capabilities": {
      "experimentalApi": false
    }
  }
}
~~~

响应：

~~~json
{
  "id": 0,
  "result": {
    "userAgent": "codex-cli/0.149.0",
    "platformFamily": "unix",
    "platformOs": "macos",
    "codexHome": "/home/user/.codex"
  }
}
~~~

确认通知：

~~~json
{
  "method": "initialized",
  "params": {}
}
~~~

### 创建 Thread

`thread/start` 没有必填字段，但生产客户端通常至少明确 `cwd`，并根据产品配置选择模型、沙箱和审批策略。

请求：

~~~json
{
  "method": "thread/start",
  "id": 1,
  "params": {
    "cwd": "/workspace/project",
    "model": "gpt-5.6-terra",
    "sandbox": "workspace-write",
    "approvalPolicy": "on-request",
    "developerInstructions": "只修改当前项目，完成后运行测试。"
  }
}
~~~

主要响应字段：

~~~json
{
  "id": 1,
  "result": {
    "thread": {
      "id": "thr_123"
    },
    "model": "gpt-5.6-terra",
    "modelProvider": "openai",
    "cwd": "/workspace/project",
    "approvalPolicy": "on-request",
    "sandbox": "workspace-write",
    "approvalsReviewer": "user",
    "instructionSources": []
  }
}
~~~

客户端必须保存 `thread.id`。不要因为页面刷新、WebSocket 断开或一段时间没有事件就创建替代 Thread。

### 启动 Turn

请求：

~~~json
{
  "method": "turn/start",
  "id": 2,
  "params": {
    "threadId": "thr_123",
    "input": [
      {
        "type": "text",
        "text": "找出失败测试的根因，修复后重新运行相关测试。"
      }
    ],
    "clientUserMessageId": "msg_bugfix_001"
  }
}
~~~

即时响应只表示 Turn 已建立：

~~~json
{
  "id": 2,
  "result": {
    "turn": {
      "id": "turn_456",
      "status": "inProgress"
    }
  }
}
~~~

后续进度通过通知到达：

~~~text
turn/started
item/started
item/commandExecution/outputDelta
item/completed
item/started
item/fileChange/patchUpdated
item/completed
item/agentMessage/delta
item/completed
turn/completed
~~~

客户端应该把 Delta 当作临时渲染，把 `item/completed` 和 `turn/completed` 当作最终事实。

### 回答审批

App Server 是双向协议。需要审批时，服务端会发送带 `id` 的请求；客户端必须用相同 `id` 返回决定。

服务端请求：

~~~json
{
  "method": "item/commandExecution/requestApproval",
  "id": 9001,
  "params": {
    "threadId": "thr_123",
    "turnId": "turn_456",
    "itemId": "item_789",
    "reason": "命令需要访问网络",
    "command": ["npm", "install"]
  }
}
~~~

客户端响应：

~~~json
{
  "id": 9001,
  "result": {
    "decision": "accept"
  }
}
~~~

不要把审批弹窗做成全局单例。请求包含 `threadId` 和 `turnId`，UI 状态也必须按 Thread 和 Turn 归属。

### 最小 Node.js 客户端

下面的例子只实现一条最小路径：拉起 App Server、完成握手、创建 Thread、发送 Turn，并等待结束。

~~~javascript
import { spawn } from "node:child_process";
import readline from "node:readline";

const server = spawn("codex", ["app-server"], {
  stdio: ["pipe", "pipe", "inherit"],
});

const lines = readline.createInterface({ input: server.stdout });
const pending = new Map();
let nextId = 1;

function send(message) {
  server.stdin.write(JSON.stringify(message) + "\n");
}

function request(method, params) {
  const id = nextId++;
  send({ method, id, params });
  return new Promise((resolve, reject) => {
    pending.set(id, { resolve, reject });
  });
}

lines.on("line", (line) => {
  const message = JSON.parse(line);

  if (message.id !== undefined && pending.has(message.id)) {
    const waiter = pending.get(message.id);
    pending.delete(message.id);
    if (message.error) waiter.reject(message.error);
    else waiter.resolve(message.result);
    return;
  }

  if (message.method === "item/commandExecution/requestApproval") {
    send({ id: message.id, result: { decision: "decline" } });
    return;
  }

  if (message.method === "turn/completed") {
    console.log("turn completed:", message.params.turn);
  }
});

await request("initialize", {
  clientInfo: {
    name: "minimal-client",
    title: "Minimal Client",
    version: "0.1.0",
  },
});

send({ method: "initialized", params: {} });

const started = await request("thread/start", {
  cwd: process.cwd(),
});

await request("turn/start", {
  threadId: started.thread.id,
  input: [{ type: "text", text: "总结这个仓库的结构。" }],
});
~~~

这个例子仍然不是生产客户端。它没有处理超时、重连、取消、全部反向请求、事件归并和业务幂等，这些将在最后一章解决。

## 命令完整指南

下面的命令表以本机 `codex-cli 0.149.0` 的 stable Schema 为准。“必填入参”只列 Schema 强制字段；空白不等于没有可选参数。“主要结果”是客户端最常用的响应内容，不替代生成的 JSON Schema。

### 连接、Thread 与分组

| 方法 | 必填入参 | 主要结果或行为 |
| --- | --- | --- |
| `initialize` | `clientInfo` | `userAgent`、平台和 `codexHome` |
| `thread/start` | 无 | 新 `thread` 与生效的 cwd、模型、沙箱、审批配置 |
| `thread/resume` | `threadId` | 恢复后的 `thread` 与生效配置 |
| `thread/read` | `threadId` | `thread`；可选包含 Turns |
| `thread/list` | 无 | `data`、分页 cursor |
| `thread/loaded/list` | 无 | 当前进程已加载的 Thread |
| `thread/fork` | `threadId` | 新 Thread；可用 `lastTurnId` 截断历史 |
| `thread/archive` | `threadId` | 归档并通知 `thread/archived` |
| `thread/unarchive` | `threadId` | 恢复 Thread 与 `thread/unarchived` |
| `thread/delete` | `threadId` | 永久删除并通知 `thread/deleted` |
| `thread/unsubscribe` | `threadId` | 当前连接停止接收该 Thread 事件 |
| `thread/name/set` | `threadId`、`name` | 更新可见名称 |
| `thread/metadata/update` | `threadId` | 更新 pin 等元数据 |
| `thread/compact/start` | `threadId` | 立即返回；压缩进度走 Item / Turn 通知 |
| `thread/rollback` | `threadId`、`numTurns` | 回滚若干 Turn 并通知 `thread/reverted` |
| `thread/inject_items` | `threadId`、`items` | 注入模型可见历史，不创建用户 Turn |
| `thread/shellCommand` | `threadId`、`command` | 启动用户明确发起的非沙箱 Shell 命令 |
| `thread/approveGuardianDeniedAction` | `threadId`、`event` | 审批 Guardian 拒绝的行动 |
| `thread/goal/set` | `threadId` | 创建或更新持久 Goal |
| `thread/goal/get` | `threadId` | 当前 Goal |
| `thread/goal/clear` | `threadId` | 清除 Goal |
| `thread/section/move` | `threadId`、`sectionId` | 移动到指定 Section |
| `threadSection/list` | 无 | Section 列表 |
| `threadSection/create` | `name` | 新 Section |
| `threadSection/update` | `sectionId`、`name` | 更新 Section |
| `threadSection/delete` | `sectionId` | 删除 Section |

#### 恢复和读取 Demo

恢复会把 Thread 加载到当前 App Server，并允许继续 `turn/start`：

~~~json
{
  "method": "thread/resume",
  "id": 10,
  "params": {
    "threadId": "thr_123",
    "cwd": "/workspace/project"
  }
}
~~~

~~~json
{
  "id": 10,
  "result": {
    "thread": { "id": "thr_123" },
    "cwd": "/workspace/project",
    "model": "gpt-5.6-terra",
    "modelProvider": "openai",
    "sandbox": "workspace-write",
    "approvalPolicy": "on-request",
    "approvalsReviewer": "user"
  }
}
~~~

只读取而不恢复：

~~~json
{
  "method": "thread/read",
  "id": 11,
  "params": {
    "threadId": "thr_123",
    "includeTurns": true
  }
}
~~~

~~~json
{
  "id": 11,
  "result": {
    "thread": {
      "id": "thr_123",
      "turns": []
    }
  }
}
~~~

### Turn 与 Review

| 方法 | 必填入参 | 主要结果或行为 |
| --- | --- | --- |
| `turn/start` | `threadId`、`input` | 新 `turn`；执行通过通知流出 |
| `turn/steer` | `threadId`、`expectedTurnId`、`input` | `turnId`；向活动 Turn 追加输入 |
| `turn/interrupt` | `threadId`、`turnId` | 请求中断；最终仍等待 `turn/completed` |
| `review/start` | `threadId`、`target` | `reviewThreadId` 与 Review Turn |

#### 追加指令

`turn/steer` 只用于仍在运行的 Turn：

~~~json
{
  "method": "turn/steer",
  "id": 20,
  "params": {
    "threadId": "thr_123",
    "expectedTurnId": "turn_456",
    "clientUserMessageId": "msg_steer_001",
    "input": [
      {
        "type": "text",
        "text": "先不要修改代码，只解释失败原因。"
      }
    ]
  }
}
~~~

~~~json
{
  "id": 20,
  "result": {
    "turnId": "turn_456"
  }
}
~~~

Turn 已结束时，不要重试 `turn/steer`，应创建新的 `turn/start`。

#### 中断

~~~json
{
  "method": "turn/interrupt",
  "id": 21,
  "params": {
    "threadId": "thr_123",
    "turnId": "turn_456"
  }
}
~~~

成功响应通常为空对象。请求成功只表示中断已提交，客户端仍要等待状态为 interrupted 的 `turn/completed`。

### 命令、文件与搜索

| 方法 | 必填入参 | 主要结果或行为 |
| --- | --- | --- |
| `command/exec` | `command` | `stdout`、`stderr`、`exitCode` |
| `command/exec/write` | `processId` | 向流式命令写 stdin |
| `command/exec/resize` | `processId`、`size` | 调整 PTY |
| `command/exec/terminate` | `processId` | 终止命令 |
| `fs/readFile` | `path` | `dataBase64` |
| `fs/writeFile` | `path`、`dataBase64` | 写入结果 |
| `fs/createDirectory` | `path` | 创建目录 |
| `fs/getMetadata` | `path` | 文件类型、大小、时间等元数据 |
| `fs/readDirectory` | `path` | 目录条目 |
| `fs/remove` | `path` | 删除结果；调用前必须做产品级确认 |
| `fs/copy` | `sourcePath`、`destinationPath` | 复制结果 |
| `fs/watch` | `path`、`watchId` | 开始监听，变化走 `fs/changed` |
| `fs/unwatch` | `watchId` | 停止监听 |
| `fuzzyFileSearch` | `query`、`roots` | 模糊文件搜索结果 |

同步命令示例：

~~~json
{
  "method": "command/exec",
  "id": 30,
  "params": {
    "command": ["git", "status", "--short"],
    "cwd": "/workspace/project",
    "timeoutMs": 10000
  }
}
~~~

~~~json
{
  "id": 30,
  "result": {
    "stdout": " M src/app.ts\n",
    "stderr": "",
    "exitCode": 0
  }
}
~~~

不要把 `command/exec` 和 Turn 中由 Agent 发起的 `commandExecution` Item 混为一谈。前者是客户端直接调用；后者属于 Agent Loop，由 Turn 的 Item 事件呈现。

### 模型、配置与权限

| 方法 | 必填入参 | 主要结果或行为 |
| --- | --- | --- |
| `model/list` | 无 | 模型、effort、输入模态和分页 cursor |
| `modelProvider/capabilities/read` | 无 | Provider 与模型组合的能力边界 |
| `permissionProfile/list` | 无 | 可用权限 Profile 与约束 |
| `experimentalFeature/list` | 无 | Feature 生命周期和启用状态 |
| `experimentalFeature/enablement/set` | `enablement` | 修改当前进程支持的 Feature 状态 |
| `config/read` | 无 | `config`、`origins` 和可选 layers |
| `config/value/write` | `keyPath`、`value`、`mergeStrategy` | 写入单个配置值 |
| `config/batchWrite` | `edits` | 批量配置写入结果 |
| `configRequirements/read` | 无 | 有效的托管配置要求 |
| `config/mcpServer/reload` | 无 | 重载 MCP 配置 |
| `windowsSandbox/readiness` | 无 | Windows Sandbox 就绪状态 |
| `windowsSandbox/setupStart` | `mode` | 启动 Windows Sandbox 配置 |

查询模型：

~~~json
{
  "method": "model/list",
  "id": 40,
  "params": {
    "limit": 50,
    "cursor": null,
    "includeHidden": false
  }
}
~~~

响应中的 `data` 才是当前账户和 Provider 真正可用的模型。不要在产品中硬编码模型或 reasoning effort 列表。

### Skills、Hooks、MCP、Plugin 与 App

| 方法 | 必填入参 | 主要结果或行为 |
| --- | --- | --- |
| `skills/list` | 无 | 按 cwd 返回 Skills |
| `skills/extraRoots/set` | `extraRoots` | 替换进程级额外 Skill 根 |
| `skills/config/write` | `enabled` | 更新 Skill 启用状态 |
| `hooks/list` | 无 | 生命周期 Hooks |
| `marketplace/add` | `source` | 添加 Marketplace |
| `marketplace/remove` | `marketplaceName` | 移除 Marketplace |
| `marketplace/upgrade` | 无 | 刷新一个或全部 Marketplace |
| `plugin/list` | 无 | Plugin 列表 |
| `plugin/installed` | 无 | 已安装 Plugin |
| `plugin/read` | `pluginName` | Plugin 详情 |
| `plugin/install` | `pluginName` | 安装 Plugin |
| `plugin/uninstall` | `pluginId` | 卸载 Plugin |
| `plugin/skill/read` | `remoteMarketplaceName`、`remotePluginId`、`skillName` | 读取远程 Skill |
| `plugin/share/save` | `pluginPath` | 保存共享 Plugin |
| `plugin/share/updateTargets` | `remotePluginId`、`discoverability`、`shareTargets` | 更新共享范围 |
| `plugin/share/list` | 无 | 共享 Plugin 列表 |
| `plugin/share/checkout` | `remotePluginId` | 拉取共享 Plugin |
| `plugin/share/delete` | `remotePluginId` | 删除共享 Plugin |
| `app/list` | 无 | 可访问 App / Connector |
| `app/read` | `appIds` | App 元数据和工具摘要 |
| `app/installed` | 无 | 已提交的 Connector Runtime 快照 |
| `mcpServerStatus/list` | 无 | MCP Server 状态 |
| `mcpServer/oauth/login` | `name` | 启动 MCP OAuth；结果由完成通知确认 |
| `mcpServer/resource/read` | `server`、`uri` | MCP Resource 内容 |
| `mcpServer/tool/call` | `server`、`threadId`、`tool` | MCP 工具结果 |

这些方法并不意味着所有能力都对每个账户、平台或配置开放。客户端应显示服务端返回的不可用原因，而不是把“Schema 中存在”误写成“当前用户一定可用”。

### 账号、反馈与外部配置迁移

| 方法 | 必填入参 | 主要结果或行为 |
| --- | --- | --- |
| `account/read` | 无 | 当前账号与 `requiresOpenaiAuth` |
| `account/login/start` | 取决于登录方式 | `loginId` 或即时结果 |
| `account/login/cancel` | `loginId` | 取消登录 |
| `account/logout` | 无 | 清除当前登录 |
| `account/rateLimits/read` | 无 | 当前 Rate Limit 快照 |
| `account/rateLimitResetCredit/consume` | `idempotencyKey` | 额度重置结果 |
| `account/usage/read` | 无 | 使用量摘要 |
| `account/workspaceMessages/read` | 无 | Workspace 消息 |
| `account/sendAddCreditsNudgeEmail` | `creditType` | 邮件请求结果 |
| `feedback/upload` | `classification` | 反馈上传结果 |
| `externalAgentConfig/detect` | 无 | 可迁移的外部 Agent 配置 |
| `externalAgentConfig/import` | `migrationItems` | `importId`；进度和完成走通知 |
| `externalAgentConfig/import/recordHistory` | `providerId`、`itemTypeResults` | 记录导入历史 |
| `externalAgentConfig/import/readHistories` | 无 | 历史导入记录 |

### 服务端反向请求

下面这些不是客户端主动命令，而是服务端发起、客户端必须回答的请求：

| 方法 | 客户端要做什么 |
| --- | --- |
| `item/commandExecution/requestApproval` | 展示命令或网络审批，并返回决定 |
| `item/fileChange/requestApproval` | 展示文件修改审批 |
| `item/permissions/requestApproval` | 展示额外文件或网络权限 |
| `item/tool/requestUserInput` | 渲染结构化问题并返回答案 |
| `item/tool/call` | 执行 Dynamic Tool 并返回内容 |
| `mcpServer/elicitation/request` | 处理 MCP 表单或 URL 确认 |
| `account/chatgptAuthTokens/refresh` | 为受支持客户端刷新 ChatGPT Auth Token |
| `attestation/generate` | 生成请求的 Attestation |
| `execCommandApproval`、`applyPatchApproval` | 兼容旧审批协议 |

回答后，服务端会发送 `serverRequest/resolved`。如果客户端没有对应 Handler，Turn 可能一直停在等待状态。

### 关键通知

客户端至少要正确归并这些通知：

| 事实 | 通知 |
| --- | --- |
| Thread 已创建或状态改变 | `thread/started`、`thread/status/changed` |
| Turn 开始和结束 | `turn/started`、`turn/completed` |
| Item 生命周期 | `item/started`、`item/completed` |
| Agent 流式文本 | `item/agentMessage/delta` |
| 命令输出 | `item/commandExecution/outputDelta` |
| 文件 Patch | `item/fileChange/patchUpdated` |
| 计划快照 | `turn/plan/updated` |
| Diff 快照 | `turn/diff/updated` |
| Token 使用量 | `thread/tokenUsage/updated` |
| 错误与警告 | `error`、`warning`、`configWarning` |

### 实验命令

使用 `generate-json-schema --experimental` 会额外暴露尚未进入稳定 Schema 的方法：

| 领域 | experimental-only 方法 |
| --- | --- |
| Bedrock 账号 | `account/bedrock/discover`、`account/bedrock/setup` |
| 协作模式 | `collaborationMode/list` |
| 执行环境 | `environment/add`、`environment/info`、`environment/status` |
| 增量文件搜索 | `fuzzyFileSearch/sessionStart`、`fuzzyFileSearch/sessionUpdate`、`fuzzyFileSearch/sessionStop` |
| Memory 与 Plugin | `memory/reset`、`plugin/search` |
| 独立进程 | `process/spawn`、`process/writeStdin`、`process/resizePty`、`process/kill` |
| Project | `project/create`、`project/read`、`project/list`、`project/update`、`project/move`、`project/import`、`project/delete` |
| Remote Control | `remoteControl/enable`、`remoteControl/disable`、`remoteControl/status/read`、`remoteControl/pairing/start`、`remoteControl/pairing/status`、`remoteControl/client/list`、`remoteControl/client/revoke` |
| 诊断 | `server/diagnostics` |
| Thread 后台终端 | `thread/backgroundTerminals/list`、`thread/backgroundTerminals/terminate`、`thread/backgroundTerminals/clean` |
| Thread 上下文与查询 | `thread/items/list`、`thread/turns/list`、`thread/search`、`thread/searchOccurrences`、`thread/revert`、`thread/memoryMode/set`、`thread/settings/update` |
| Thread Elicitation 计数 | `thread/increment_elicitation`、`thread/decrement_elicitation` |
| Thread Queue | `thread/queue/add`、`thread/queue/list`、`thread/queue/update`、`thread/queue/reorder`、`thread/queue/delete`、`thread/queue/start` |
| Thread Realtime | `thread/realtime/start`、`thread/realtime/stop`、`thread/realtime/appendText`、`thread/realtime/appendAudio`、`thread/realtime/appendSpeech`、`thread/realtime/listVoices` |

Schema 中还包含 `mock/experimentalMethod`，它用于实验协议测试，不应成为产品集成入口。

这些方法只有在 `initialize.params.capabilities.experimentalApi = true` 且当前构建实际支持时才应调用。实验方法必须做版本探测、失败关闭和升级回归，不能成为业务持久状态的唯一入口。

## 如何接入产品

协议能跑通，只完成了最容易的一半。生产客户端真正要拥有的是状态归属、故障恢复和人类决策。

<Figure
  src="/images/posts/codex-app-server-guide/client-architecture.svg"
  alt="生产级 Codex App Server 客户端分层架构，包含业务界面、协议客户端、交互决策、传输层和 App Server"
  caption="客户端应把业务状态、协议状态、用户决策和传输生命周期分开，避免把所有处理塞进一条消息回调。"
/>

### Transport

负责启动或连接 App Server、按 JSONL 或 WebSocket frame 解码消息、处理关闭和重连。它不知道业务 Task，也不判断 Turn 是否成功。

### RPC Client

分配请求 ID，维护 Pending Promise，处理响应、超时和取消。断线时必须拒绝或迁移所有未完成 Promise，不能让调用永久等待。

### Server Request Router

按方法分发审批、用户输入、Dynamic Tool 和 MCP Elicitation。每个 Handler 都要带上原请求 `id`、`threadId` 和 `turnId`。

### Event Reducer

使用 `threadId + turnId + itemId` 归并事件：

- Delta 更新临时 UI；
- `item/completed` 覆盖 Item 最终值；
- `turn/completed` 结束 Turn；
- `serverRequest/resolved` 清理待处理交互；
- `error` 和不可恢复状态进入可见故障面。

### 业务层

业务系统至少保存：

~~~text
businessObjectId
threadId
activeTurnId
clientUserMessageId / 幂等键
业务验收状态
关键事件快照或游标
~~~

不要让 `turn/completed` 直接驱动“需求完成”“发布成功”或“QA 通过”。Codex 只知道 Agent Turn 的状态，不知道你的业务合同。

### 恢复流程

~~~text
启动或连接 App Server
  → initialize / initialized
  → 从业务库读取 threadId
  → thread/read 确认 Thread 存在
  → thread/resume 恢复运行上下文
  → 校验 cwd、模型、沙箱和 instructionSources
  → 读取当前状态
  → 决定继续 turn/start、等待活动 Turn 或进入人工处理
~~~

不要因为连接断开就创建新 Thread。先区分：

- 请求是否已被 App Server 接收；
- Turn 是否仍在运行；
- 事件是否只是没有送达当前连接；
- Thread 是否仍在持久化存储中；
- 业务动作是否具备幂等保护。

### 最常见的错误

| 现象 | 根因 | 修正 |
| --- | --- | --- |
| 请求全部报 Not initialized | 没完成握手 | 等待 `initialize` 响应后发送 `initialized` |
| UI 一直显示运行中 | 只消费 Delta | 用 `item/completed`、`turn/completed` 收敛 |
| Turn 永远不结束 | 反向请求无人处理 | 实现 Server Request Router |
| 页面刷新后任务丢失 | 浏览器被当成事实源 | 在服务端保存 `threadId` 和业务映射 |
| `turn/steer` 冲突 | Turn 已结束或 ID 不匹配 | 重新读取状态，必要时新建 Turn |
| 升级后字段失败 | 客户端硬编码旧 Schema | 重新生成 stable / experimental Schema |
| 远程 WebSocket 暴露风险 | 无 TLS 或 Auth | 使用 `wss://` 和握手认证 |
| 业务提前宣布完成 | 把 Turn 完成当业务验收 | 业务层独立保存验收事实 |

### 维护这份集成

每次升级 Codex：

1. 记录 `codex --version`；
2. 重新生成 stable 与 experimental Schema；
3. 对比 Initialize、Thread、Turn、反向请求和事件定义；
4. 用真实 Thread 验证握手、恢复、Turn、审批和完成事件；
5. 只在验证后开放新增能力。

继续阅读：

- [OpenAI App Server 官方文档](https://developers.openai.com/codex/app-server/)
- [OpenAI：Unlocking the Codex harness](https://openai.com/index/unlocking-the-codex-harness/)
- [Codex App Server 开源实现](https://github.com/openai/codex/tree/main/codex-rs/app-server)

App Server 最值得记住的不是某个方法名，而是所有权边界：Codex 拥有 Thread、Turn、Item 和执行事实；你的产品拥有用户、业务对象、验收状态和失败恢复策略。把这条边界分清，App Server 才会从“能调用”变成“能长期运行”。
