---
title: 开源的身体：DeepSeek Harness 深度解读
date: 2026-09-01
lastmod: 2026-09-03
summary: DeepSeek 开源 Harness：智能体循环、工具、会话日志乃至界面都是可替换插件。本文实跑 0.1.1-rc.2 并核对 0.1.2-alpha.5 源码，拆解配置树、可逆效果、工具管线与只追加账本，以及替换能到哪一层。
tags:
  - DeepSeek
  - Harness
  - Agent Runtime
  - Agent Infra
featured: false
draft: false
---

2026 年 8 月 13 日，一个名为 DeepSeek Harness 的项目登上 Hacker News 首页，拿到七百多个赞。但高赞评论里最醒目的一条是个疑问："所以这到底是什么？它居然能上第一，有点奇怪。README 除了安装说明几乎什么都没有。"（[Hacker News 讨论](https://news.ycombinator.com/item?id=49285244)）

这个困惑不丢人，它点中了盲区：DeepSeek 这次发布的不是一个能立刻上手的聊天产品，也不是一个新模型，而是一层大多数人从没听说过的东西——harness，把模型变成智能体的运行系统。官方发布页只用几行字介绍它，把其余一切指向了代码仓库。

为了弄清楚它到底是什么，我做了三件事：把官方架构文档、插件教程和 Cordis 教程完整读了一遍；读了支撑整个设计的[论文](https://arxiv.org/abs/2608.25512)；然后在本地把它装起来跑了一遍，把真实的运行配置抓了出来。本文就是这次拆解的结果。

<Callout title="版本边界">
本地实跑为 npm 包 `@deepseek-ai/dsh` 0.1.1-rc.2（2026-09-01）；机制部分另核对了 2026-09-03 的 GitHub 源码（提交 `49a606b`，仓库版本 `0.1.2-alpha.5`）。同一时刻 npm 的 `latest` 仍指向 `0.1.1-rc.2`、`alpha` 指向 `0.1.2-alpha.3`——源码文档里出现的新概念，安装包里未必有，先核对版本。项目自称开发者预览（developer preview），README 用大写字母警告"未来将出现破坏兼容性的变更"。
</Callout>

## 模型是灵魂，Harness 是身体

先说清楚 harness 是什么。大模型本身只做一件事：接收一段文本，返回一段文本。它可能在回复里说"我需要调用一个工具"，但它自己不能真的去执行——不能读文件、不能跑命令、不记得两步之前发生了什么，更不知道哪些操作应该先问你一声。

这些模型之外的活，都由一层运行系统完成：组装上下文、暴露工具、执行命令、施加权限、保存状态、再把结果带回下一次模型调用。这层系统就是 harness。DeepSeek 官方发布页给出的公式是 "Agent = Model + Harness"，并解释说"模型是智能体的灵魂，harness 让智能体理解环境、使用工具，并在真实环境中持续工作"。

一个类比：模型像一位只会做决策的司机，harness 是整辆车——引擎、传动、刹车和行车记录仪。同一位司机开不同的车，能到达的地方完全不同；车决定了意图能变成什么。我们熟悉的 Claude Code、Codex CLI、OpenCode，本质上都是各自模型外面的车（本博客对 Codex 那辆车的内部构造有一篇完整拆解：[在意图与行动之间：Codex Harness 如何工作](/posts/codex-agent-runtime)）。

由此得到一个判断：AI 助手的能力上限不再只由模型决定，harness 决定模型的"聪明"能兑现成多少行动。但这里要压住一个容易膨胀的结论：可替换不等于效果更好。DeepSeek Harness 允许换模型、换工具、换循环，开放的是控制面和实验面，不是自动提高任务正确率的开关。

## 一份配置文件就是一个 Agent

市面上的 harness 大多是成品：你可以往上加工具、加插件，但骨架——执行循环、会话管理、权限系统——封死在代码里，看不见也动不了。DeepSeek 的 Harness（命令行工具叫 `dsh`）把骨架也做成了数据。

我在本地装好 0.1.1-rc.2 后，运行了一条官方文档建议的命令：

```bash
dsh --profile web --dump-config
```

它打印出这次启动实际装配出来的完整插件树——503 行 YAML。节选如下（包括行首的层标注，这是输出原样）：

```yaml
# == @deepseek-ai/dsh-base
- id: timer
  name: '@deepseek-ai/cordis-plugin-timer'
- id: llm
  name: '@deepseek-ai/dsh-llm'
- id: session
  name: '@deepseek-ai/dsh-session'
- id: agent-default-model
  name: '@deepseek-ai/dsh-agent-default-model'
  config:
    provider: deepseek-official
    model: deepseek-v4-flash
# == @deepseek-ai/dsh-base, patched by @deepseek-ai/dsh-web-app
- id: sandbox-policy
  name: '@deepseek-ai/dsh-sandbox-policy'
  config:
    mode: workspace-write
# ……共 503 行，界面、工具、权限、遥测全在其中
```

这份输出值得逐层看。所谓"一切皆插件"在这里不是口号，是可以逐行数出来的事实：

- **默认模型是一行配置**。`deepseek-v4-flash`，来自 `deepseek-official` 提供方。想换成任何 OpenAI 兼容端点，改这一行。
- **权限体系是三档预设**：`read-only`、`workspace-write`、`danger-full-access`，默认 `workspace-write`；审批策略默认是"问"，只有显式选了全权限档才会变成"从不问"。
- **界面本身是三十多个 `ui-*` 插件**：主题、布局、侧边栏、对话视图、工具卡片、子 Agent 面板、轨迹视图……你看到的 Web 界面，和模型适配器一样，也是树上可替换的一行。
- **连"计划模式"的提示词都是配置**。`plan-mode` 这一行的 `config.section` 字段里，放着一整段告诉模型"你现在处于计划模式、先探索再提交计划"的系统提示。产品文案即配置数据。

组装规则在[架构文档](https://github.com/deepseek-ai/deepseek-harness/blob/main/docs/architecture.md)里写得很清楚：一次运行的 `dsh` 是一棵按顺序叠加的插件树，配方（Profile）列出它叠哪些套件（Bundle），每层补丁（Patch）按 `id` 定位一行并整行替换配置，后写的赢。官方出厂五个配方：带浏览器界面的 `web`、一次性执行的 `headless`、供程序调用的 `sdk` 系列、面向自动化协议的 `acp`。日常启动只需一条命令：

```bash
npx @deepseek-ai/dsh web
```

本地起一个 Web 界面（默认 `http://127.0.0.1:3080`），填入 DeepSeek API key、选定工作目录，就能让 agent 读写文件、执行命令、拆解任务。到这一步，它用起来和其他编程助手没有区别。

### 组合是两层：Profile 装应用，Preset 装 Agent

核对当前源码会发现，上面说的组装发生在进程层，而组合实际是两层。Profile 组装整个应用：带不带 Web Server、Session 存在哪种后端、Host 上有哪些 Provider。另一层叫 Agent Preset，组装单个 Session 里的 Agent：这个会话能看到哪些工具、提示词片段和 Skills。官方随包四个 Preset：

| Preset | 模型看到的能力 |
| --- | --- |
| `standard` | 文件、Shell、搜索、Skills、计划、Goal、子 Agent 与 Workflow |
| `ptc` | 完整 Coding Agent，但主要通过 `run_code` 和生成的 TypeScript SDK 组合工具 |
| `minimal` | 持久 Bash 与 `str_replace_editor` 两个核心工具 |
| `cordis` | 在 Standard 上加入运行时检查、插件实验和 Preset 创作能力 |

两层分开回答的是一个真实问题：同一个 Web 进程承载多个 Session，如果工具都挂在进程全局，一个 Session 为自己增加能力，其他 Session 也会一起变化。现在能力隔离发生在 Session 作用域，安全策略留在 Host——Agent 不能通过换 Preset 给自己放宽权限。

有一条硬限制值得记住：Preset 只能在 Session 还没产生消息或工具调用时切换。一段历史里如果已经出现旧工具调用，中途换成另一套工具和提示词，恢复与重放就失去了稳定解释。这条限制把"一切都能动态换"的口号压回了会话一致性。官方还把它列进了[已知限制](https://github.com/deepseek-ai/deepseek-harness/blob/49a606bc5b5934603f22a26957a07dc799ab0291/packages/preset/agent-presets/README.md#known-limitations-and-deferred-work)：自定义 Preset 靠复制内置组合而来，没有"继承 Standard 再改一行"的补丁语义，复制出来的组合会随上游演进而漂移。

四个 Preset 与其说是四种产品模式，不如说是四组对照实验：改变工具丰富度、改变工具呈现与组合方式、减少 harness 提供的先验能力、把运行时组合本身暴露给 Agent。

本地起一个 Web 界面之后，区别在于树的深处。有一个细节是我这次运行才注意到的：配置行的排列顺序没有加载语义，某个插件何时激活，由它声明依赖的服务是否就绪决定。换句话说，这棵树不是按顺序执行的脚本，而是一张按依赖关系自行点亮的网络。这句话的分量，读完论文那节会更清楚。

那么"插件"本身是什么？官方[插件教程](https://github.com/deepseek-ai/deepseek-harness/blob/main/docs/user/develop/basic/index.md)给的最小形态是一个导出 `apply` 函数的 TypeScript 模块：

```ts
import type { Context } from '@deepseek-ai/cordis'

export const name = 'hello-plugin'

export function apply(ctx: Context) {
  console.log('[hello-plugin] plugin loaded!')
}
```

框架加载插件时调用 `apply`，传入一个共享的 `ctx` 上下文对象，插件通过它注册能力。整个项目的扩展方式就这一个入口：往 `ctx` 上挂。

## 每个零件都有撤销键

任何做过插件系统的人都知道，真正的难题不是"能不能插上"，而是：运行到一半把零件拆下来，它留下的注册、订阅和副作用怎么办？一个工具注册了、一个事件监听挂上了、一段提示词注入了，如果卸载时清不干净，系统会越用越脏，"热插拔"就永远只是演示。

DeepSeek 的答案写在它底层的插件框架 [Cordis](https://github.com/cordiverse/cordis) 里：**一切注册都是可逆的效果（revertible effect）**。教程里的例子很直白——插件起了一个定时器：

```ts
export function apply(ctx: Context) {
  ctx.effect(() => {
    const timer = setInterval(() => console.log('tick'), 200)
    return () => clearInterval(timer) // 卸载时自动执行
  })
}
```

`ctx.effect()` 里放获取资源的代码，返回释放它的函数；插件卸载时，运行时替你调用释放。更关键的是，大多数时候你根本不需要手写这一层：`ctx.on()` 挂的事件监听、通过 `ctx.tools.register()` 注册的工具，本身就已经是效果——监听器随插件卸载自动消失，没有人需要手动记账。

每个加载中的插件实例在运行时有一个状态机：

```text
PENDING → LOADING → ACTIVE → UNLOADING → DISPOSED
              ↘ FAILED
```

卸载不是把对象扔掉，而是沿状态机走完 `UNLOADING`：所有清理函数按注册的逆序执行（异步清理并发进行），递归卸载它挂载的子插件，全部完成后才到 `DISPOSED`。

这套机制有正式的学术表述。支撑 Cordis 的论文 [_A Programming Paradigm for Spatiotemporal Composability_](https://arxiv.org/abs/2608.25512)（北京大学与 DeepSeek 合作，2026 年 8 月发布于 arXiv，cs.PL 方向）把上面的设计命名为**时间可组合性**（temporal composability）：组件被移除时，其副作用能够被完整撤销。论文的做法是把经典的"效果"概念提升为运行时机制——每一次上下文变换都携带一个逆操作，由运行时统一持有；于是撤销不再依赖组件作者的自觉，而是系统的结构性保证。

论文摘要的第一句话也值得抄下来，它直接点明了动机："现代软件——从插件系统到自我演化的智能体 harness——越来越需要动态组合，但其形式化基础仍然发育不足。"一家模型实验室为插件的热插拔写一篇编程语言方向的论文，这件事本身就在说明它把 harness 当成了什么：不是产品，是需要形式化基础的系统问题。

一位读完论文的 Hacker News 用户给出了第三方评价："它给插件系统加上了热重载和动态启用/卸载能力……他们把它推得更远，一直推到 UI 组件。"这是社区视角的判断，不是官方承诺；但和我在配置树里看到的三十多个 `ui-*` 插件对得上。

当然这仍然不是魔法。可逆是一种编程约束：副作用必须走 Cordis 能追踪的入口；插件自己绕过 `ctx.effect()` 修改外部系统，运行时不能凭空生成撤销操作。远程请求、已写进数据库的数据、已经发出去的消息，都不会因为插件卸载而回滚。论文保证的是"注册和监听清得干净"，不是分布式事务。

## 零件自己决定何时上线

时间维度解决"拆得干净"，空间维度解决"装得起来"。插件树动辄几百行，谁依赖谁？传统做法是手工编排启动顺序，而手工编排的顺序一定会在某个夜晚骗你。

Cordis 的做法是：插件用 `inject` 声明需要的服务，运行时等所有依赖就绪才加载它。需要 `tools` 服务的插件，在 `ctx.tools` 存在之前停在状态机的 `PENDING` 格子里；哪个服务先上线，依赖它的插件就先点亮。这就是论文说的**空间可组合性**（spatial composability），机制名叫"响应式余效果"（reactive coeffect）：上下文的每一次变化，都会对照每个组件声明的需求分类，驱动它的激活与去激活。我在配置树里注意到的"行序没有加载语义"，正是这条机制在配置层的投影；文件系统实现消失时，依赖它的插件会退出 Active，服务恢复后再重新装载。

组件之间的通信则用类型化事件。事件的调度模式是契约的一部分，共五种：广播观察（`emit`）、层层包裹的中间件（`waterfall`）、并行（`parallel`）、顺序取首个有效值（`serial`）、一票否决（`bail`）。其中 `waterfall` 是拦截能力的来源：每个监听者拿到参数和一个 `next()` 延续，可以改写传递下去的内容，也可以不调用 `next()` 直接否决整条链。

这套抽象落到 harness 上，就是处处可插拔的拦截点。架构文档里最重要的两条规则：

- `agent/pre-step` 是瀑布事件，**决定模型能看到什么**。监听者可以改写即将进入模型的消息，也可以整体否决；被否决的输入仍会关闭一个持久化的 turn，日志里留下这次尝试的记录。
- 扩展插件只依赖 `agent` 接口，**永远不直接依赖 `agent-loop`**——文档原话是"这样循环本身保持可换"。也就是说，智能体的执行循环是一个默认实现，不是唯一的实现。

再接上能力接缝（seam）：一个能力由接口定义、实现方、消费方三个角色组成，换掉实现方，整组能力跟着迁移。文档举的例子是把文件系统和子进程的实现指向远程沙箱，Bash、终端和 LSP 会一起搬过去，不需要为每个工具单独改造。配置树里 `sandbox`、`subprocess`、`subagent` 各自独立成行，就是为这种整体替换预留的位置。

论文给这一层补上了最后一块理论：它证明在上述机制下，不同组件的效果可以交错执行而互不干扰（论文称之为观测等价之上的组合演算），并把可组合性从单个组件推广到整个交错运行的系统。通俗地说：它想证明"几百个插件同时热插拔"这件事在原理上不会互相踩脏。至于证明与工程实现之间还有多远，没有人验证过，包括我。

## 从一句话到一条命令之间

理解了骨架，再看一次真实的工具调用要穿过什么。官方[工具执行管线](https://github.com/deepseek-ai/deepseek-harness/blob/main/docs/tool-execution-pipeline.md)把这条路径画成了流程图，整理出来是这样：

```text
模型回复中出现工具调用
  → tool/call 先记入日志（参数是模型产出的原始 JSON）
  → tools/pre-execute 瀑布：hooks、权限、沙箱策略依次过手
  → 单调守卫：只能否决或弃权，身份受保护
  → 审批提示：无人应答或答不了，按否决处理
  → tools/execute 瀑布：超时、重试、指标包裹真正的执行
  → 工具本体执行（写文件先过 fs/* 意图闸门）
  → tools/post-execute 瀑布：放行、阻断、替换结果或追加上下文
  → 最终结果冻结为不可变记录 → tool/result 记入日志
```

几个设计选择值得普通读者留意。第一，**先记账后执行**：`tool/call` 事件在工具真正运行之前就写进日志，记录的是模型产出的原始参数，不经过任何美化——即使审批拒绝、沙箱不可用或工具抛错，日志仍能回答模型曾经试图做什么。第二，**审批失败按否决处理**，而不是按放行：审批服务缺席或无法应答时默认拒绝，这个方向选对了安全的那一边；人工批准也只放行当前这一次调用。第三，**收紧是单调的**：审批之后的单调守卫（Monotonic Guard）只能进一步否决或弃权，不能把上游已拒绝的调用改回允许——后挂载的插件无法靠事件顺序偷偷放宽所有者的策略。第四，权限、钩子、沙箱都不写死在循环里，而是挂在三个瀑布事件上；换一个权限方案，是挂一个新的监听者，不是改执行循环。

权限这块还有两个旋钮值得分清。界面上的"权限预设"不是一个模糊的安全等级，而是 Sandbox Mode 与 Approval Policy 的组合：前者决定进程的文件写入范围，后者决定哪些调用需要人回答；这层服务只记录选择，本身不执行安全检查。沙箱的词汇表也只约束文件效果，网络和进程可见性不在其中：本地实现在 Linux 用 bwrap 或 Landlock，macOS 用 Seatbelt，Windows 用 ACL Restricted Token，并如实上报覆盖是 `full` 还是 `partial`；受限模式找不到可用后端时必须报 `SANDBOX_UNAVAILABLE`，不能静默退化成无沙箱执行。[Sandbox 文档](https://github.com/deepseek-ai/deepseek-harness/blob/main/docs/subsystems/sandbox.md)

这条管线发生在一个叫 turn 的结构里：一个 step 是一次模型请求加上它调用的工具，一个 turn 是零个或多个 step。用户的一句话进来，循环开始认领输入、组装上下文、请求模型、执行工具，直到没有欠下的动作为止。你中途插话，新消息进入同一个 turn 的收件箱；你反悔，取消会留下 `interrupted` 标记。

## 模型看到的一切都有账本

对普通使用者来说，AI 助手最让人不安的问题之一是：它到底看了什么、做了什么。Harness 把可追溯做成了架构级的承诺。

官方发布页写道："每一次运行都可追溯。模型看到的一切都被记录在只追加（append-only）的会话日志里。"[会话子系统的文档](https://github.com/deepseek-ai/deepseek-harness/blob/main/docs/subsystems/session.md)把这句话收紧成一条硬性不变量：**"模型可见即已记录"**（Model-visible means logged）——任何能进入模型请求的内容，必须能从会话日志重建出来，运行时还有断言持续校验这一点。

日志的事件词汇表本身就是设计的一部分：

| 事件 | 记录什么 |
| --- | --- |
| `turn/start` / `turn/end` | 轮次的打开与结束原因 |
| `step/start` / `step/end` | 一次模型请求和它的工具 |
| `user/message` | 进入模型的输入及其来源（人、注入的上下文、目标续轮） |
| `assistant/chunk` | 原始流式块，保留到 token 级 |
| `assistant/message` | 组装后的回复、token 用量、中断标记 |
| `tool/call` / `tool/result` | 模型发出的原始调用参数与最终结果 |
| `request/header` | 当次生效的模型配置、渲染后的 System Prompt 与 Tool Schema |

注意三件事。模型的对话历史**不从别处存取，而是从这条日志推导出来**——回放就是重新推导，恢复和分叉也基于同一条事件流，所以不存在"历史文件"和"日志"对不上的问题。同时 `assistant/chunk` 把流式输出的原始块也留在日志里，这意味着回放可以精确到逐 token 重建当时的界面。

第二件事是表里最后一行。`request/header` 很容易被忽略，但它回答的问题最硬：模型是在什么运行环境里说这些话的。Prompt 和工具集合会随插件、Preset、权限说明和模型路线变化；只存用户与助手文本，恢复出来的只是"说过什么"，不是"当时模型看到了什么"。快照只在包络发生变化时追加，连续请求继承最近的 Header——这是重建完整性与日志体积之间的取舍。顺带一提，动态组装对 KV Cache 并不友好：频繁装卸插件、重排 Tool Schema 会从变化位置破坏稳定前缀，可组合性把 token 和延迟稳定性也变成了运行时设计问题。

第三件事是恢复的诚实度。如果日志停在 `turn/start` 之后、`turn/end` 之前，恢复过程会为缺失的工具结果和结束标记补上 Interrupted 记录，再从新的 Turn 继续；对"外部工具可能已产生副作用、但结果没写回"的情况，运行时不假装它没执行，也不盲目重放，而是标记未知、从一个完整的新边界继续。[Persistence 文档](https://github.com/deepseek-ai/deepseek-harness/blob/main/docs/subsystems/persistence.md)

我在本地配置树里也看到了这套机制的落盘形态：`session-persistence-jsonl` 把会话写成主目录下的 JSONL 文件，`session-query-sqlite` 维护查询索引，`session-projection` 负责把事件折叠成客户端视图，遥测默认关闭、可指向自建端点。日志、索引、投影三层各司其职——这几乎是把"事件溯源"教科书写进了出厂配置。

核对源码时我还厘清了一对容易混的概念：`turn/start`、`assistant/message`、`tool/result` 属于 **Session Event**，是已经发生的持久事实；`agent/pre-step`、`tools/pre-execute` 属于 **Cordis Event**，是运行中的扩展点，插件可以在当下观察、改写或否决一次动作，但事件本身不进聊天历史。区分的实践含义很直接：想在工具执行前拦截，挂活跃事件；想让新增上下文在恢复后仍能重建，就必须新增一种 Session Event。把短暂 Hook 当持久记录，重启后信息就消失了。

UI 也只是日志的读者。Web 界面的工具卡片、Diff、终端输出都从 `tool/call`、`tool/result` 和持久化 metadata 投影而来，投影函数必须是纯函数——重放时不能重新读文件、看当前时钟或发网络请求。这条限制解决的是时间错位：一次 Edit 在周一改了文件，周五重放时 UI 若再去读当前文件，展示的 Diff 已经不是周一那次修改的证据。

这条约束的含金量在于它排除了暗角：不存在"模型看到了但日志里没有"的输入。提示词注入、工具结果、中途塞进来的上下文，凡是影响过模型决策的东西都有账可查。当然，账本能提高可追溯性，不能自动证明每条记录都是真的：工具可能返回错误内容，外部系统也可能在日志写入后继续变化。它保存的是 harness 观察到的运行事实，不是整个世界的真相。将来评估任何 AI 助手，都可以拿这句"模型可见即已记录"当标尺问一问。

## 开发者能替换到哪一层

理解骨架之后，真正的问题是：我的项目该动哪一部分？官方把扩展入口的归属写得很清楚：

| 想改变什么 | 应使用的入口 |
| --- | --- |
| 增加模型路线 | 在 `ctx.llm` 注册 Adapter |
| 增加模型工具 | 在 `ctx.tools` 注册 Tool |
| 改变一个 Session 的工具与提示词 | 创建 Agent Preset |
| 注入下一次模型请求的上下文 | `agent.inject()` |
| 改写或拒绝模型请求 | `agent/pre-step`、`agent/request` |
| 在工具前后加权限、超时或审计 | `tools/pre-execute` / `execute` / `post-execute` |
| 更换文件、Shell 或沙箱实现 | 替换对应 Capability Provider |
| 增加可恢复的 Session 状态 | 扩展 `SessionEventMap` 并实现 Projection |
| 让 UI 显示新的工具卡片 | 从 Session Event 与持久 metadata 做纯投影 |

表背后是前面提过的能力接缝：一项可替换能力由接口定义、实现 Provider、消费方三个角色组成，只有接口没有 Provider 不产生行为，只有 Provider 没有消费方则模型无从调用。官方强调完整的接缝需要三种角色齐备，而不是建一个抽象类就宣布"可插拔"。

两处更深的替换值得单独说。**PTC**：Standard Preset 下模型逐个调用工具，读十个文件就是十次往返；PTC Preset 把工具生成为 TypeScript SDK，主要向模型暴露 `run_code`，把一串调用折进一段程序，中间结果不进对话。但这不是免费的 token 节省——生成的 SDK 同样占上下文，子调用仍穿过普通工具管线并记录日志；而且代码运行时的 Worker Thread 提供的是 containment，不是 security boundary，模型生成代码的信任级别接近 Bash。**多 Agent** 则被拆成两条接缝：Subagent Provider 决定"子任务交给谁"——同进程新建、从父 Session 的完整 Turn 边界 Fork、经 ACP 启动外部 Agent、甚至拉起 Codex 或 Claude Code 子进程；Workflow Engine 决定"怎么排班"——模型提交一段只用于编排的 JavaScript，跑在没有文件和网络直接所有权的 VM Context 里，读写落在它启动的子 Agent 上。能并行启动一百个 Agent，只证明调度接口存在，不证明一百份结果会自动组成正确答案。

放错层的代价也要说清：扩展点丰富意味着开发者必须判断一项行为属于短暂拦截、模型可见上下文、持久事实、底层 Provider 还是 UI 投影。放错层依然能"跑起来"，但恢复、隔离或重放时会出现缺口。

## 实验室们真正在争什么

把视角拉远一点。Harness 发布之前，编程助手形态的 agent 已经竞争了一年多。Hacker News 上有一条被反复引用的评论："到此为止，最后一家发布了值得用来写代码的模型、却没有自家 harness 的实验室，也交卷了。"

这句话点破了格局：模型厂商几乎都在补齐自己的 harness，因为模型与 harness 协同得越好，产品的兑现能力越强。但 DeepSeek 的选择和这条常规路径不太一样。官方把 Harness 定位为"面向全世界 agent harness 开发者的开发者预览"——它不是 DeepSeek 模型的专属驾驶舱，而是一个任何人都可以拿来组装自己 agent 的开放运行时。配置树本身就是证据：默认模型只是其中一行，`llm` 服务是个接缝，换掉它不需要碰任何源码。与此同时，官方维护的 [awesome-deepseek-agent](https://github.com/deepseek-ai/awesome-deepseek-agent) 列表显示，DeepSeek 的模型已经被接入了二十多个第三方工具，从 Claude Code、Codex 到各种开源终端助手。

这两件事放在一起，可以读出一种策略——需要声明，这是我的推断而非官方表态：DeepSeek 延续了它"把重资产开源"的一贯动作，只是这次开源的不是模型权重，而是"组装智能体的权利"。如果开发者基于 Cordis 和 `dsh` 长出自己的 harness，DeepSeek 的模型就自然地坐在这个生态的中心。发布约 19 天，仓库收获超过 20 万 star、2.3 万 fork（2026-09-01 观察），说明注意力已经到位。

社区也有冷静的质疑。有用户在 HN 上问：第一方 harness 搭配自家模型，真的有优势吗？目前没有任何公开证据表明 DeepSeek 的模型为 Harness 做过协同训练，这个问题仍然是开放的。

## 这个预览版还不能承诺什么

注意力不等于成熟度。在决定认真对待它之前，有几条边界必须看清。

它会变。开发者预览阶段，官方明确预告破坏兼容性的变更；我实跑的包版本叫 `0.1.1-rc.2`，仓库里的基础套件已经是 `0.1.2-alpha.5`，npm 的 dist tag 和 Git tag 在同一天都可能不一致。今天写的插件和配置，明天可能需要重写。

它没有被安全审计过。项目自带的[安全说明](https://github.com/deepseek-ai/deepseek-harness/blob/main/SAFETY.md)写得很直白：它能执行模型生成的代码和命令、加载第三方插件、访问网络和凭证，"不能被视为安全的或可上生产的"；沙箱与审批能降低风险，但不保证隔离，也不能保护已经允许访问的资源。官方的建议是最小权限、一次性虚拟机或容器、先备份、审查每一个插件和命令。我在配置树里看到的那档权限预设名叫 `danger-full-access`——连命名都在提醒你它存在。

范式不等于体验。"一切皆插件"解决的是架构的可换性，不直接回答"用起来顺不顺"。503 行的配置树对设计者是自由，对新手是墙；README 的简陋和概念的密度，都还在消耗社区的好感。一位 HN 用户读完论文后的评价很典型："可能有用，但没有那么有用。"

所以它适合谁，也要说清楚。如果目标只是使用一个成熟的 Coding Agent，它当前的抽象与变化成本可能大于收益；如果你在研究 Agent Runtime、需要替换执行环境、构建多种 Session 组合，或想把模型、工具、历史、权限和 UI 拆成可独立演化的模块，它提供了一套少见的完整实验场。采用者要承担的工作是实打实的：跟随接口变化、维护自定义组合、审查第三方插件、为 Provider 和策略写验证，并在 Harness 之外建立真实任务的验收。

回到开头那个问题："这到底是什么？"现在可以给一个完整的答案：它是 DeepSeek 对下一层竞争的下注——agent 的未来不该只有一个又一个封闭成品，还应该有一套谁都能拼装、每个零件都可替换、每笔账都可追溯的公共运行时。他们用一篇论文给"可替换"打底，用一份 503 行的配置树给"一切皆插件"举证。

看完 DeepSeek，再评价其他 Agent 产品时，可以少问一句"用了什么模型"，多问三件事：它允许替换什么？替换之后，模型看到和做过的事情能否重建？从工具意图到真实副作用之间，谁拥有最后决定权？

这个判断成立与否，取决于一个可以观察的条件：接下来几个月，`dsh-plugin` 话题下能不能长出真实繁荣的插件生态。如果只有 star 没有插件，它就只是一次漂亮的工程实验；如果生态长出来了，8 月 13 日这一天可能会被记住——不是因为又一个 AI 产品发布，而是因为 AI 助手的"身体"第一次成了可以逐行检阅的公共基础设施。
